class Solution:
    def climbStairs(self, n: int) -> int:
        l = [0,1,2,3]
        k = n
        i = 3
        while n>3:
            l.append(l[i] + l[i-1])
            i += 1
            n -= 1
        return l[k]
