import math
class Solution:
    def myPow(self, x: float, n: int) -> float:
        y = x
        return math.pow(x,n)
        # if n>1:
        #     for i in range(n-1):
        #         y = x*y
        #     return y
        # elif n==0:
        #     return 1
        # elif n<0:
        #     for j in range(abs(n)-1):
        #         y = x*y
        #     return (1/y)
        # else :
        #     return x
